Process utilisé pour résoudre le 502 Bad Gateway
  hypothèses envisagées :
   1- la VM ne fonctionne pas correctement
   2- la gateway ne fonctionne pas
   3- le reverse proxy de la gateway ne fonctionne pas
   
 I déployement de l'infrastructure minimum avec une VM qui héberge uniquement NGINX.
 NGINX est un serveur web qui écoute et répond sur le port 80.
 La VM posséde une adresse IP public pour pouvoir y accéder en directe sans passer par la gateway.
 
 résultat : l'interrgogation de la VM par son adresse IP public renvoie la page d'accueil de NGINX, 
            l'interrogation de la VM en passant par l'IP public de la gateway renvoie une réponse identique.
   -> la gateway renvoie bien vers la VM en backend sur le port 80.
   
  II installation de jenkins en standalone sur la VM
    1 - la gateway renvoie une erreur : 502 BAD gateway
    2 - verification des paramètres de la gateway : changement du port du backend_http_settings en 8080 
                                                    pour établir le reverse proxy
          -> pas de changement pour la gateway erreur 502
          -> la VM présente la page d'initialisation du mot de passe administrateur de Jenkins par son IP public
     3 - connection en ssh sur la VM : exploration des logs. Utilisation de la commande "tcpdump" pour voir le traffic des paquet échangés entre la VM et la Gateway.
            "tcpdump" révèle une communication entre la VM et la Gateway mais la VM renvoie un : 403 forbiden.
            
            -> l'application gateway possède une probe par défaut qui n'accepte que des réponse comprise entre 200 et 299.
            Comme jenkins renvoie une réponse 403, la gateway concidère la VM comme down et affiche donc une erreur 502
            
            la mise en place d'une probe personnalisée vers la page d'initialisation du mot de passe administrateur permet d'afficher Jenkins en passant par la gateway et de lever l'erreur 502.
   
